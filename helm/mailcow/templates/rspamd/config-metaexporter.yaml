apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "mailcow.fullname" . }}-rspamd-metaexporter
  namespace: {{ .Release.Namespace }}
data:
  {{- $searchBase := "data/conf/rspamd/meta_exporter/" -}}
  {{- $search := "*" -}}
  {{- range $path, $bytes := .Files.Glob (printf "%s%s" $searchBase $search) }}
  {{ $path | replace $searchBase "" }}: | {{ $.Files.Get $path | nindent 4 -}}
  {{- end -}}
